server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # react-router-dom uses client-side routing, so unknown paths must
    # fall back to index.html instead of a 404 from nginx.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
